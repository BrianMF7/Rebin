{
  "name": "rebin-pro-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ReBin Pro edge processing",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240925.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^3.78.12"
  }
}
